package com.prasad.banking.entity;

/**
 * Represents the type of bank account.
 *
 * SAVINGS — Standard savings account (interest-bearing).
 * CURRENT — Business/current account (no interest, higher transaction limits).
 */
public enum AccountType {
    SAVINGS,
    CURRENT
}
